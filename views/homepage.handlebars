{{!-- TODO: get this functional --}}
{{!-- {{#if itemAdd}}
    <div class="p-2 width border">
        <h2>Shopping Cart</h2>
        <div class="container">
        {{#each itemAdd as |item|}}
            <div class="row">
                <p class="col">x{{count}}</p>
                <p class="col">{{name}}</p>
                <p class="col">${{price}}</p>
            </div>
        {{/each}}
        </div>
        <h4>Total = ${{sum.price}}</h4>
    </div>
{{/if}} --}}

{{#if displayCart}}
    <div class="p-2 width border">
        <h2>Shopping Cart</h2>
        <div class="container">
            {{#each itemAdd as |item|}}
            <div class="row">
                {{!-- <p class="col">x{{count}}</p> --}}
                <p class="col">{{name}}</p>
                <p class="col">${{price}}</p>
            </div>

            {{/each}}
        </div>

    </div>
{{/if}}

<div class="gallery">
    {{#each menuItems as |menu|}}
    {{> menu-details}}
    {{/each}}
</div>

<script src="./js/cart.js"></script>

